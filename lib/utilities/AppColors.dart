import 'package:flutter/cupertino.dart';

class AppColors {
  static const background = Color(0xFFF7F7F7);
  static const card = Color(0xFFFFFFFF);

  static const title = Color(0xFF1C1C1C);
  static const body = Color(0xFF6B7280);

  static const primary = Color.fromARGB(255, 255, 154, 3);
  static const icon = Color(0xFF4B5563);

  static const danger = Color(0xFFEF4444);
  // Backgrounds
  static const scaffold = Color(0xFFF7F7F7);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1C1C1C);
  static const textSecondary = Color(0xFF6B7280);

  // Accentsr
  static const secondary = Color(0xFF7C7CFF); // voice note

  // Icons / borders
  static const divider = Color(0xFFE5E7EB);

  // Convenience colors
  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);
  // Variants and tints
  static const primaryVariant = Color(0xFFFFA807);
  static const inputFill = Color(0x8DFFE1DB); // ARGB(141,255,225,219)
}
